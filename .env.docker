# Local Docker only — never points at production RDS.
# Used as a reference; docker-compose.yml sets these explicitly.
DATABASES_URL=mysql://kalapatru:kalapatru@db:3306/kalptaru1
SECRET_KEY=local-docker-dev-secret-not-for-prod
DEBUG=True
ALLOWED_HOSTS=*
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
BOOTSTRAP_ADMIN_USER=admin
BOOTSTRAP_ADMIN_PASSWORD=admin123
